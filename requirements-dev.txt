# Development and tooling dependencies (install with `pip install -r requirements-dev.txt`)

# Linting / formatting / type checking
flake8>=6.0.0,<7.0.0
mypy>=1.0.0,<2.0.0
ruff>=0.5.0,<1.0.0

# Testing
pytest>=7.0.0,<9.0.0

# Database migrations (optional but recommended)
alembic>=1.13.0,<2.0.0

